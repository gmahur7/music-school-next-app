/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['images.unsplash.com','pexels.com','unsplash.com','res.cloudinary.com'],
    }
};

export default nextConfig;
